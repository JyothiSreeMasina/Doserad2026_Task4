# root-level only -- do NOT match src/data/, which is real source code
# the build needs (COPY src/ in Dockerfile)
/data/
/checkpoints/
/logs/
/notebooks/
/archive/

.venv/
__pycache__/
*.pyc
.git/
Hugging Face Token.txt
*.log

# packaged submission artifacts and per-task weight/test dirs -- large,
# and not needed inside the build context for any task's image
*.tar.gz
docker*/model/
docker*/test-data/
paper/
